{
  "_description": "CSS variables in :root that may legitimately differ between a base CN template and its EN locale variant (-en). Anything not listed here must match across the base and variant.",
  "always_allowed": [
    "--serif",
    "--sans",
    "--mono",
    "--latin-ui"
  ],
  "per_pair_allowed": {
    "_doc": "key = base template name (e.g. 'one-pager'); value = list of additional CSS variables that may differ for any variant of that base"
  }
}
